public enum Rarity { Common, Rare, Epic, Legendary }

public enum UpgradeEffect
{
    // Player
    MaxHPFlat,
    MoveSpeedPct,
    MagnetRangeFlat,
    // Weapons (global)
    DamagePct,
    CooldownPct,       // negative value reduces cooldown
    ProjectileCountPlus,
    PiercePlus,
    AreaPct
}